# things you probably want to change
# url is the website domain url without a trailing slash
url: https://ebenji.github.io/portfolio/
baseurl: portfolio
# name of website
title: Ethan Benjamin
description: Ethan Benjamin's Online Presence
nav-links:
  - name: Home
    url: /
  - name: About
    url: /about
  - name: Blog
    url: /blog
  - name: Lessons
    url: /lessons

# things you probably don't want to change
defaults:
  - # applies the default layout to all files in the project
    scope:
      path: ""
    values:
      layout: "default"
  - # sets is_post=true for all blog posts
    scope:
      path: ""
      type: posts
    values:
      is_post: true
permalink: /:title # so we have /about instead of /about.html, and /blog-post instead of /2019-11-22-blog-post.html
sass:
  style: compressed # compress the CSS file generated from the SCSS styles file